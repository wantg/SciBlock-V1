现在先暂停继续加新功能，先对当前项目做一次“代码结构梳理 + 模块化收口建议”。目标是避免后期出现牵一发动全身的局面。

这次不要直接重构代码，先做一次真实结构审计和收口方案设计。必须基于当前项目的真实文件与依赖关系输出，不要泛泛而谈。

审计目标
请判断当前项目是否已经做到：
- 功能模块边界清楚
- 页面层 / hook 层 / API adapter 层 / context 层 / 展示组件层分离
- 新功能接入成本可控
- 不会因为一个小改动波及多个不相关模块
- 关键上下文（尤其 WorkbenchContext）没有继续失控膨胀

请按下面 8 个部分输出：

一、按功能域重新梳理当前项目结构
请基于真实代码，把当前项目按功能域列出来，例如但不限于：
- Auth / User session
- Home / Recent feeds
- SciNote
- Personal workbench
- Team / Members
- Team / Member experiment read-only
- Reports
- Calendar
- Attachment display
- Shared types / adapters / utils

对于每个功能域，请列出：
1. 主要目录
2. 关键文件
3. 当前职责
4. 是否存在“本该属于该域的逻辑散落在别处”的问题

二、找出当前最危险的高耦合点
请列出项目里最可能导致“牵一发动全身”的 Top 10 文件或模块，并说明：
- 文件路径
- 当前承担了哪些职责
- 哪些职责本不该混在一起
- 后续改它最容易波及哪些地方

重点请特别检查：
- WorkbenchContext.tsx
- SciNoteStoreContext.tsx
- UserContext.tsx
- MemberDetailPage.tsx
- MemberExperimentDetailPage.tsx
- App.tsx
- api/ 下各 adapter 文件
- pages/team/detail/ 下的组件

三、检查我们最近几轮改动后的结构是否真的收口了
请重点检查这几条链路是否已经形成稳定边界：

1. recent experiment feed
- 是否真正独立于 SciNoteStoreContext
- 是否遵守 adapter 规则
- NoteCard/RecentNotes 是否还残留旧语义

2. 成员视角实验链路
- 是否已完全脱离 useSciNoteStore / WorkbenchContext
- 是否真正属于 /home/members/ 域
- 是否还存在任何 personal/member 语义混用

3. 附件只读展示
- 是否复用了纯只读组件
- 是否有编辑逻辑泄漏到成员详情页

4. UserContext 会话同步
- 当前修复是否完整
- 是否还存在角色状态与 token 脱节的风险

四、按“改动场景”评估未来影响范围
请模拟下面这些常见改动，告诉我每种大概要动几层、哪些文件、风险大小：

1. 新增一个实验状态，并让它同时出现在：
   - 日历
   - recent experiment feed
   - 成员实验详情页
2. 修改 SciNote 返回字段名
3. 给成员实验详情页新增一个“项目标题”面包屑层
4. 给实验记录列表加分页
5. 给附件加下载能力
6. 给成员视角详情页补只读的附件预览

请对每个场景判断：
- 影响范围是 小 / 中 / 大
- 目前结构是否可接受
- 是否会出现明显的跨模块耦合风险

五、检查是否还存在这些危险信号
请逐项明确 yes / no，并给真实证据：

1. 页面组件里仍塞了太多业务逻辑
2. Context 仍同时承担数据获取、业务判断、视图拼装
3. 展示组件仍依赖过多业务上下文
4. 同一实体仍有多套命名混用
5. 路由参数语义仍有模糊之处
6. 原始 ApiXxx 类型仍泄漏到页面层
7. 旧逻辑 / 过渡逻辑 / 废弃路径未清理干净
8. 某些功能其实已经做成了“看起来分层，实际强耦合”

六、给出当前项目的模块化评分
请从以下维度打分（1-10）并说明原因：
1. 功能分层清晰度
2. API adapter 边界清晰度
3. 类型系统一致性
4. 状态管理边界
5. 页面与业务逻辑解耦程度
6. 新功能接入成本
7. 团队可维护性
8. 可继续扩展性

最后给一个总评，并明确回答：
- 当前项目更像“可维护产品”
还是
- “逐渐变复杂但还可收口的项目”
还是
- “已经高风险耦合”

七、给出最值得优先做的 5 个结构收口动作
不要泛泛而谈，要具体到：
- 问题点
- 影响
- 建议怎么收
- 涉及哪些文件
- 风险高低
- 是否建议立刻做

请特别判断这些是不是现在优先级高：
- report 从 WorkbenchContext 解耦
- AI assist 从 WorkbenchContext 解耦
- calendarRecords.ts 的目录归属调整
- 成员域 API / hook / 页面再进一步聚合
- auth storage key 的统一管理

八、最后给一个明确结论
请直接回答这三句话：
1. 这个项目现在是否已经出现“牵一发动全身”的趋势
2. 最主要的根因是什么
3. 下一步最应该做的是：
   - 继续开发功能
   - 边开发边小步收口
   - 先做一轮集中结构收口

注意
- 这次只做结构梳理和建议，不直接重构
- 结论必须基于真实代码
- 不要为了好看而弱化问题
- 我要的是对后期可维护性有帮助的真实判断