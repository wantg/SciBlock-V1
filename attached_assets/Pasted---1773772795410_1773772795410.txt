现在做一轮“阶段性结构盘点”。这次不是继续改功能，而是基于当前真实代码，重新评估项目整体结构状态，确认最近这一批功能开发和小步解耦之后，项目是否比之前更健康了，以及下一轮最值得收口的地方在哪里。

要求
必须基于当前真实代码和真实依赖关系输出，不要泛泛而谈，也不要重复上一次审计的套话。我要看的是：在 recent feed、成员实验链路、附件只读展示、UserContext 同步、成员权限收紧、report/AI assist 解耦、周报模块切主路径之后，项目当前的结构状态到底变成了什么样。

请按下面 8 个部分输出：

一、当前项目的结构状态总览
请先给一句总体判断：
- 项目现在是更健康了，还是只是继续堆功能
- 相比上一次结构审计，最大的变化是什么
- 哪些区域明显变好了
- 哪些区域依然是主要风险点

二、按功能域重新梳理现状
请按当前真实代码，把项目重新分成这些功能域（如果你认为还应调整分类，可以补充）：
- Auth / User session
- Home / Recent feeds
- SciNote
- Personal workbench
- Team / Members
- Team / Member experiment read-only
- Personal reports（自动汇总周报）
- Legacy manual reports compatibility
- Calendar
- Attachment display
- Shared types / adapters / utils

对每个功能域，说明：
1. 主要目录
2. 关键文件
3. 当前边界是否清楚
4. 是否还存在逻辑散落或跨域耦合

三、重新评估高耦合点 Top 10
请重新列出当前最值得警惕的高耦合文件 / 模块 Top 10，并说明：
- 文件路径
- 现在为什么仍然危险
- 与上次相比是变好还是没变
- 后续如果再改它，最可能波及哪些地方

重点请特别重新评估：
- WorkbenchContext.tsx
- UserContext.tsx
- SciNoteStoreContext.tsx
- MyReportsPage.tsx
- AiReportDetailPanel.tsx
- api/weeklyReport.ts
- App.tsx
- MemberDetailPage.tsx

四、检查最近几轮收口是否真的生效
请逐项判断这几件事是否已经真正改善了结构，而不是表面移动代码：

1. report 第一阶段从 WorkbenchContext 解耦
2. AI title assist 从 WorkbenchContext 解耦
3. recent experiment feed 的 adapter 边界
4. 成员视角实验链路脱离 personal/workbench 主上下文
5. UserContext 跨 iframe 会话同步
6. 周报模块切换到“自动汇总为主，手动周报兼容”
7. 周报模块死代码清理

对每一项回答：
- 是否真的收口成功
- 结构收益是什么
- 还残留什么尾巴

五、按“未来改动场景”重新评估风险
请模拟下面这些改动，告诉我当前项目下影响范围和风险：

1. 给实验状态再新增一个状态
2. 给成员实验详情页加“项目标题”面包屑层
3. 给自动汇总周报加“重新生成”按钮
4. 给周报详情页增加“AI 建议”区块
5. 给附件增加下载能力
6. 给 workbench 自动报告链继续做 Phase 2 解耦

对每个场景判断：
- 影响范围 小 / 中 / 大
- 主要会动哪些层
- 当前结构是否能承受
- 有没有明显的耦合风险

六、检查还存在的危险信号
请对下面这些信号逐项给 yes / no，并附真实证据：

1. 页面组件仍然塞了太多业务逻辑
2. Context 仍在承担不该承担的 UI 状态
3. 展示组件仍直接依赖重业务上下文
4. 原始 ApiXxx 类型仍泄漏到不该出现的层
5. 路由参数或 ID 语义仍有混淆风险
6. 历史链路虽然退主线，但仍容易误用
7. 某些功能“看起来分层，其实强耦合”
8. 仍有值得立刻删除/移动/收紧的文件或接口

七、给当前项目重新打分
请从 1-10 打分，并说明理由：
1. 功能分层清晰度
2. API adapter 边界清晰度
3. 类型系统一致性
4. 状态管理边界
5. 页面与业务逻辑解耦
6. 新功能接入成本
7. 团队可维护性
8. 可继续扩展性

最后给出总评，并明确判断：
- 当前项目更像“可维护产品”
- 还是“仍在继续复杂化，但还能收口”
- 还是“已经进入高风险耦合”

八、下一轮最值得做的 5 个动作
请给出当前最值得优先处理的 5 个结构动作，必须具体到：
- 问题点
- 为什么现在值得做
- 涉及哪些文件
- 风险高低
- 是否建议立刻做

请特别判断这些候选项当前优先级：
- report 自动触发链 Phase 2 解耦
- AiReportDetailPanel section 拆文件
- 周报 DB 迁移正规化
- memberSciNotes / memberExperiment 进一步聚合
- WorkbenchContext 内 editor bridge / flow draft 的边界梳理

最后请用三句话直接回答：
1. 这个项目现在是不是比上一次审计时更健康了
2. 当前最主要的结构债是什么
3. 下一步应该是继续开发功能，还是先做一轮集中收口